# Copy this file to .env and fill in real values. Never commit .env.

# PostgreSQL connection string
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://g2g_h35f_user:INgV6QjXUM7HiWLfdsO0BnRkGWY19nPJ@dpg-dad9e0ojo6nc73dkpuc0-a.oregon-postgres.render.com/g2g_h35f

# Set to "true" only if your Postgres host requires SSL (most managed hosts do,
# e.g. Render, Railway, Supabase, Neon, RDS)
DATABASE_SSL=true

# Secret used to sign JWTs — use a long random string in production
# generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change-this-to-a-long-random-string

# How long a login session lasts
JWT_EXPIRES_IN=7d

# Server port
PORT=4000

# Comma-separated list of origins allowed to call this API with credentials.
# For local dev with the frontend served by this same server, this can stay as-is.
CORS_ORIGIN=http://localhost:4000

# "production" enables secure cookies (requires HTTPS)
NODE_ENV=development
